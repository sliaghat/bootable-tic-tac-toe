# Bootable-Tic-Tac-Toe
A classic game written entirely in x86 assembly that runs directly on bare metal. Features an unbeatable AI powered by the minimax algorithm, VGA text‑mode output, and PS/2 keyboard input. Boot via GRUB and challenge a machine that never loses.
